# dependencies / build
node_modules/
dist/

# packaged extension
*.mcpb

# secrets & local runtime state
.env
tokens/
audit/

# the real field allow-list is user-supplied; only the example is tracked
config/field-allowlist.json

# os
.DS_Store
